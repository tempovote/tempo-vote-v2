# =============================================================================
# tempo-vote-v2 — Environment Variables
# Copy to .env and fill in values. Never commit .env to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Cardano Infrastructure — Preprod Testnet
# -----------------------------------------------------------------------------
OGMIOS_PREPROD_URL=ws://localhost:1337
KUPO_PREPROD_URL=http://localhost:1442

# -----------------------------------------------------------------------------
# Cardano Infrastructure — Mainnet
# -----------------------------------------------------------------------------
OGMIOS_MAINNET_URL=ws://ogmios-mainnet:1337
KUPO_MAINNET_URL=http://kupo-mainnet:1442

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:password@localhost:5432/tempo_vote

# -----------------------------------------------------------------------------
# IPFS — Pinata (DRep metadata anchors, CIP-119)
# -----------------------------------------------------------------------------
PINATA_JWT=
PINATA_GATEWAY=https://gateway.pinata.cloud

# -----------------------------------------------------------------------------
# Blockfrost — DRep delegator stake indexer (whale leaderboard)
# Register at https://blockfrost.io — one project per network, free tier is sufficient.
# Without these, the whale leaderboard shows no data.
# -----------------------------------------------------------------------------
BLOCKFROST_MAINNET_PROJECT_ID=mainnetXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
BLOCKFROST_PREPROD_PROJECT_ID=preprodXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# -----------------------------------------------------------------------------
# API Server
# -----------------------------------------------------------------------------
API_PORT=8080
API_SECRET=change-me-in-production
# Set to true to enable on-chain vote indexer (streams full chain via Ogmios WebSocket — high bandwidth)
VOTE_INDEXER_ENABLED=false

# -----------------------------------------------------------------------------
# Next.js (apps/web)
# These are exposed to the browser — no secrets here
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_DEFAULT_NETWORK=preprod
